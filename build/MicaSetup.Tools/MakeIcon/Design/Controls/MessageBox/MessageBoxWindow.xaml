<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MakeIcon.Design.Controls"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    xmlns:wui="http://schemas.lepo.co/wpfui/2022/xaml">

    <SolidColorBrush x:Key="ContentDialogForeground" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ContentDialogBackground" Color="#272727" />
    <Thickness x:Key="ContentDialogBorderWidth">0</Thickness>
    <SolidColorBrush x:Key="ContentDialogBorderBrush" Color="#272727" />
    <GridLength x:Key="ContentDialogButtonSpacing">8</GridLength>

    <Style TargetType="{x:Type local:MessageBoxWindow}">
        <Setter Property="MinWidth" Value="30" />
        <Setter Property="MinHeight" Value="20" />
        <Setter Property="Title" Value="{Binding Title}" />
        <Setter Property="wui:Design.Background" Value="{DynamicResource ApplicationBackgroundBrush}" />
        <Setter Property="wui:Design.Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
        <Setter Property="ExtendsContentIntoTitleBar" Value="True" />
        <Setter Property="Foreground" Value="{DynamicResource ContentDialogForeground}" />
        <Setter Property="Background" Value="{DynamicResource ContentDialogBackground}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ContentDialogBorderWidth}" />
        <Setter Property="BorderBrush" Value="{DynamicResource ContentDialogBorderBrush}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="SizeToContent" Value="WidthAndHeight" />
        <Setter Property="ResizeMode" Value="NoResize" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:MessageBoxWindow}">
                    <Grid x:Name="Container">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="24,24,24,0">
                            <TextBlock FontSize="21" Text="{TemplateBinding Caption}" />
                        </Grid>
                        <Grid Grid.Row="1" Margin="24,16,24,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <wui:FontIcon Grid.Column="0"
                                          VerticalAlignment="Top"
                                          FontFamily="{DynamicResource SymbolThemeFontFamily}"
                                          FontSize="36"
                                          Foreground="#4CC2FF"
                                          Glyph="{x:Static local:FontSymbols.Info}" />
                            <ScrollViewer Grid.Column="1">
                                <TextBlock Margin="16,8,0,0" Text="{TemplateBinding Message}" />
                            </ScrollViewer>
                        </Grid>
                        <Grid Grid.Row="2"
                              Margin="24,48,0,-16"
                              HorizontalAlignment="Stretch">
                            <Grid x:Name="CommandSpace" KeyboardNavigation.DirectionalNavigation="Contained">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="PrimaryColumn" Width="*" />
                                    <ColumnDefinition x:Name="FirstSpacer" Width="{StaticResource ContentDialogButtonSpacing}" />
                                    <ColumnDefinition x:Name="SecondaryColumn" Width="*" />
                                    <ColumnDefinition x:Name="SecondSpacer" Width="{StaticResource ContentDialogButtonSpacing}" />
                                    <ColumnDefinition x:Name="CloseColumn" Width="*" />
                                </Grid.ColumnDefinitions>
                                <Button x:Name="OKButton"
                                        Grid.Column="0"
                                        HorizontalAlignment="Stretch"
                                        Content="OK" />
                                <Button x:Name="YesButton"
                                        Grid.Column="0"
                                        HorizontalAlignment="Stretch"
                                        Content="Yes" />
                                <Button x:Name="NoButton"
                                        Grid.Column="2"
                                        HorizontalAlignment="Stretch"
                                        Content="No" />
                                <Button x:Name="CancelButton"
                                        Grid.Column="4"
                                        HorizontalAlignment="Stretch"
                                        Content="Cancel" />
                            </Grid>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
